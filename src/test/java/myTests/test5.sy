
int m(int a[][3], int c, int d[]) {

    int sum = 0;
    int i = 0;
    while (i < 2) {
        int j = 0;
        while (j < 3) {
            sum = sum + a[i][j];
            j = j + 1;
        }
        i = i + 1;
    }
    return sum + c + d[1]; // 32
}

int main(){
    int a[2][3] = {{1, 2, 3}, {4, 5, 6}};
    return m(a, a[1][2], a[1]);
}
